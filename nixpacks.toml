# Tells Railway to install ffmpeg (needed by moviepy)
[phases.setup]
nixPkgs = ["ffmpeg", "python311"]

[start]
cmd = "gunicorn server:app --bind 0.0.0.0:$PORT --workers 1 --worker-class gthread --threads 4 --timeout 600"
